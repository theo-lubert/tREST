{
  "name": "@trestjs",
  "version": "0.0.1",
  "description": "End-to-end typesafe REST APIs, the tRPC way",
  "author": "Théo Lubert",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.9.1",
    "@typescript-eslint/parser": "^6.9.1",
    "eslint": "^8.53.0",
    "prettier": "^3.0.3",
    "tsx": "^3.14.0",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  },
  "scripts": {
    "clean": "pnpm --parallel -r run clean",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "rebuild": "pnpm run clean && pnpm --filter core run build && pnpm -r i && pnpm build && pnpm run test"
  }
}
